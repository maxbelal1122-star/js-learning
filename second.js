/* create a const object called profile to store information shown in the picture. */
let profile = {
  follow:false,
  post:10,
  follower:50,
  following:4,
  stingstore: "Shradha_khapra"
};
console.log(profile);